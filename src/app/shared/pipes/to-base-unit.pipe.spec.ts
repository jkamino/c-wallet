import { ToBaseUnitPipe } from './to-base-unit.pipe';

describe('ToBaseUnitPipe', () => {
  it('create an instance', () => {
    const pipe = new ToBaseUnitPipe();
    expect(pipe).toBeTruthy();
  });
});
